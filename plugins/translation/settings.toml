# settings.toml — Translation plugin settings
# Edit this file to change default behaviour. Changes take effect on the next run.

[translation]
# Which models to use, best first. More than one on purpose: providers retire
# models, and a second and third choice means translation carries on working
# instead of stopping until someone picks a new default. If none are left, the
# sandbox falls back to the cheapest model that fits — which keeps things
# working, but chooses on price alone, so keep this list current.
models = ["gpt-4o", "gemini-2.5-pro", "gpt-4o-mini"]
temperature = 0.5          # 0.0 = fully deterministic, 2.0 = maximum creativity
top_p = 0.5                # Nucleus sampling threshold (lower = more focused output)
max_tokens = 4000          # Maximum response tokens (increase for very long pages)
context_percentage = 0.65  # Fraction of the previous page passed as context (0.0–1.0)

[image_translation]
# Must be able to read pictures, so every model here needs vision support.
models = ["gpt-5", "gpt-4o", "gemini-2.5-pro"]
temperature = 0.3          # Slightly creative to handle ambiguous characters via context
max_tokens = 8000          # Higher budget: output includes both transcript and translation
